# UI (Qt — bản chính: app_qt.py)
PySide6-Essentials>=6.6

# Thu âm + phím tắt + xuất chữ
sounddevice>=0.4
numpy>=1.24
pynput>=1.7
pyperclip>=1.8

# Dịch nhanh (Realtime): bắt âm thanh hệ thống (WASAPI loopback, Windows).
# Dịch text qua Google Cloud Translation REST v2 bằng urllib stdlib (không cần
# SDK google-cloud-translate nặng — giữ app nhẹ máy đúng triết lý cloud-only).
PyAudioWPatch>=0.2.12.7

# Nhận dạng: Groq API (đám mây) — gọi bằng urllib stdlib, không cần thư viện thêm.
# Bản cloud-only: KHÔNG dùng faster-whisper / CUDA nữa.

# Dịch realtime độ trễ thấp (tùy chọn): streaming STT qua Deepgram WebSocket.
# stdlib không có client websocket nên cần lib này; chỉ dùng khi bật chế độ streaming.
websocket-client>=1.7

# (bản cũ pywebview giữ lại làm tham khảo: app.py)
pywebview>=5.0
pystray>=0.19
pillow>=10.0
